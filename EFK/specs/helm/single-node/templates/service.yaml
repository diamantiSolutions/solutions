apiVersion: v1
kind: Service
metadata:
  name: {{ template "single-node.fullname" . }}
  labels:
    app: {{ template "single-node.name" . }}
    chart: {{ template "single-node.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "single-node.name" . }}
    release: {{ .Release.Name }}
